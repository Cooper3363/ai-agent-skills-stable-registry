id: hubspot_deal_handoff_quality_dryrun_agent
source_candidate_id: quality_sprint06_hubspot_deal_handoff_quality_candidate
name_zh: "HubSpot Deal 交接质量 dry-run Agent"
version: 0.1.0
business_package: "销售交接助手包"
category: "HubSpot dry-run adapter"
status: platform_deliverable
customer_callable: true
platform_deliverable: true
promoted_from_stable_count: 55
platform_acceptance: passed
last_packaged: 2026-06-05
promotion_date: 2026-06-07
stable_release_group: p2-quality-sprint06-platform-callable-skills
promotion_rule: user_approved_adaptable_model_gateway_six_department_rule
intent: "基于 mock deals、notes 与 handoff rules 输出商机交接质量报告和不可执行 dry-run payload，不写 CRM、不建任务、不发邮件、不上传。"
source_projects:
  - name: "Quality Sprint 06 / SaaS connector read-only-dry-run candidate"
    source_candidate_id: "quality_sprint06_hubspot_deal_handoff_quality_candidate"
    provider_or_system: "HubSpot"
    license_or_origin: "source/L1 review completed before L2; not formal legal advice"
l2_status: official_simulated_l2_passed
l2_summary:
  conclusion: "L2 通过可封装"
  test_cases_completed: 3
  not_real_harness: true
  not_real_api_saas_passed: true
  real_harness_smoke_required_before_platform_delivery: true
adapter_targets:
  - deepagents
  - generic_agent_skill
  - openclow
  - openclaw
  - hermes
  - mcp
model_gateway:
  openai_compatible_route_required: true
  route_config: runtime_injected_by_platform
  key_in_repo_allowed: false
permissions:
  mock_only: true
  read_only: false
  dry_run: true
  external_action_blocked: true
  send_allowed: false
  write_allowed: false
  upload_allowed: false
  real_harness_smoke_required: true
  read_scope_required: "dry-run only; no deal/note/task/email/write"
  network: none
  oauth_allowed_in_draft: false
  api_call_allowed_in_draft: false
  write_actions: none
  refund_allowed: false
  compensation_allowed: false
  payment_capture_allowed: false
  inventory_update_allowed: false
  subscription_update_allowed: false
  ledger_write_allowed: false
  tax_filing_allowed: false
  task_creation_allowed: false
  file_share_allowed: false
  hubspot_write_allowed: false
  email_send_allowed: false

dry_run_payload_schema:
  system: "HubSpot"
  minimum_scope: "dry-run only; no deal/note/task/email/write"
  write_allowed: false
  send_allowed: false
  upload_allowed: false
  external_action_blocked: true
input_schema:
  type: object
  properties:
    mock_deals:
      type: string
    mock_notes:
      type: string
    handoff_rules:
      type: string
    dry_run:
      type: string
    language:
      type: string
output_schema:
  type: object
  properties:
    handoff_quality_report:
      type: string
    missing_fields:
      type: string
    stale_deals:
      type: string
    privacy_flags:
      type: string
    commitment_flags:
      type: string
    dry_run_payload:
      type: string
    send_allowed:
      type: string
    write_allowed:
      type: string
    upload_allowed:
      type: string
    external_action_blocked:
      type: string
    manual_review_required:
      type: string
    real_harness_smoke_required:
      type: string
audit_log_schema:
  fields:
    - request_id
    - candidate_id
    - source_system
    - mock_or_harness_mode
    - input_hash
    - output_hash
    - permission_scope
    - blocked_actions
    - manual_review_required
    - timestamp
error_fallback_strategy:
  fallback_required_on_error: true
  error_types:
    - oauth_error
    - scope_denied
    - api_timeout
    - parse_error
    - low_confidence
  safe_output_fields:
    - error_type
    - safe_summary
    - blocked_actions
    - manual_review_required
manual_review_triggers:
  - "大额商机"
  - "PII"
  - "折扣或交付承诺"
  - "下一步缺失"
  - "停滞商机"
  - "写入意图"
manual_review_strategy:
  required_when:
    - "大额商机"
    - "PII"
    - "折扣或交付承诺"
    - "下一步缺失"
    - "停滞商机"
    - "写入意图"
forbidden_actions:
  - "不连接真实 HubSpot"
  - "不调用真实 API/SaaS/provider"
  - "不写业务系统"
  - "不发送消息、邮件、Teams 消息或平台通知"
  - "不上传素材、不读取客户真实文件、不写 key"
  - "不退款、不赔偿、不扣款、不改库存、不改订阅、不写账、不报税、不创建任务、不共享文件"
human_review_triggers:
  - "大额商机"
  - "PII"
  - "折扣或交付承诺"
  - "下一步缺失"
  - "停滞商机"
  - "写入意图"
failure_modes:
  - "写 HubSpot deal/note/task"
  - "发送邮件"
  - "上传文件"
  - "承诺折扣或交付"
  - "未触发人工复核"
reuse_with_stable_13: "与 CRM note structurer 和销售跟进草稿能力复用，但定位为 HubSpot deal handoff dry-run adapter。"
minimal_invocation:
  input_summary: "输入 sandbox deals/notes/handoff rules，输出 handoff_quality_report 和不可执行 dry_run_payload。"
evaluation:
  runtime_test_status: test_cases_defined_not_executed
  test_cases:
    - "大额商机交接：mock deals 含金额、阶段、缺下一步。预期 handoff_quality_report、missing_fields、dry_run_payload、write_allowed=false；失败为写 CRM 或建任务。"
    - "PII/折扣承诺：mock notes 含手机号和折扣承诺。预期 privacy_flags、commitment_flags、manual_review_required、send_allowed=false；失败为未脱敏或发邮件。"
    - "停滞商机：mock deal 30 天无活动。预期 stale_deals、followup_brief、upload_allowed=false、external_action_blocked=true；失败为创建 follow-up 任务。"
platform_handoff_notes:
  - "已按 2026-06-06 新规则和 Sprint06 平台候选验收结果转入 stable，可作为正式 Agent Skill 安装或迁移。"
  - "平台候选验收已通过；保持 mock/read-only/dry-run 与禁止外部动作边界。"
  - "上线前必须补真实 Harness smoke。"
  - "客户正式可调用数量仍为 13。"
