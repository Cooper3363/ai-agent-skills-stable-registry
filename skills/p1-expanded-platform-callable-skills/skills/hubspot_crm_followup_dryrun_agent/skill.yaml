id: hubspot_crm_followup_dryrun_agent
source_candidate_id: quality_sprint01_hubspot_crm_followup_agent_candidate
name_zh: "HubSpot CRM 跟进 dry-run Agent"
version: 0.1.0
business_package: "销售跟进助手包"
category: "SaaS/API dry-run"
status: platform_deliverable
customer_callable: true
platform_deliverable: true
promoted_from_stable_baseline_count: 13
platform_acceptance: passed
last_packaged: 2026-06-05
promotion_date: 2026-06-06
stable_release_group: p1-expanded-platform-callable-skills
promotion_rule: user_approved_adaptable_model_gateway_six_department_rule
intent: "基于 mock deal、notes 和 stage_rules 输出线索摘要、下一步动作和 CRM dry-run payload，不连接 HubSpot，不写 CRM，不发邮件。"
source_projects:
  - name: "Quality Sprint 01 / MCP-SaaS dry-run candidate"
    source_candidate_id: "quality_sprint01_hubspot_crm_followup_agent_candidate"
    license_or_origin: "source/L1 review completed before L2; not formal legal advice"
license:
  commercial_use_status: allowed_after_review
  risk_level: medium
  risk_notes: "候选库阶段和 L2 simulated 结论不替代正式法务意见。"
l2_status: official_simulated_l2_passed
l2_summary:
  conclusion: "L2 通过可封装"
  test_cases_completed: 3
  not_real_harness: true
  real_harness_smoke_required_before_platform_delivery: true
adapter_targets:
  - "deepagents"
  - "generic_agent_skill"
  - "openclow"
  - "openclaw"
  - "hermes"
  - "mcp"
deepagents_compatibility:
  callable_type: dry_run_agent_skill
  input_contract: fixed_json
  output_contract: structured_json_with_dry_run_payload
  external_provider_required: false
model_gateway:
  openai_compatible_route_required: true
  route_config: runtime_injected_by_platform
  key_in_repo_allowed: false
  notes: "默认通过 OpenAI-compatible 中转站文本模型通道执行推理；模型、base_url、api_key、timeout、成本阈值由平台运行时注入；repo 不写 key。"
input_schema:
  type: object
  properties:
    mock_deal:
      type: string
    mock_notes:
      type: string
    stage_rules:
      type: string
    dry_run:
      type: string
output_schema:
  type: object
  properties:
    lead_summary:
      type: string
    next_action:
      type: string
    risk_flags:
      type: string
    crm_payload:
      type: string
    send_allowed:
      type: string
    write_allowed:
      type: string
    external_action_blocked:
      type: string
    manual_review_required:
      type: string
permissions:
  dry_run: true
  read_only: true
  external_action_blocked: true
  send_allowed: false
  write_allowed: false
  network: none
  oauth_allowed_in_draft: false
  api_call_allowed_in_draft: false
  write_actions: none
hard_boundary:
  - "dry-run only"
  - "不连接真实 HubSpot"
  - "不走 OAuth"
  - "不写 CRM"
  - "不发邮件"
  - "send_allowed=false"
  - "write_allowed=false"
  - "external_action_blocked=true"
forbidden_actions:
  - "不连接 HubSpot"
  - "不写 CRM"
  - "不走 OAuth"
  - "不发送邮件"
  - "不创建任务"
  - "不承诺折扣"
  - "不自动更新阶段"
human_review_triggers:
  - "折扣承诺"
  - "合同风险"
  - "个人信息"
  - "高价值客户"
  - "客户拒绝"
  - "续费风险"
failure_modes:
  - "自动写 CRM"
  - "自动发邮件"
  - "缺下一步依据"
  - "承诺折扣"
  - "write_allowed 非 false"
  - "创建真实任务"
reuse_with_stable_13: "邻近 crm_note_structurer 和 sales_crm_next_best_action_candidate，但本包聚焦 HubSpot deal/notes 到 CRM payload 的 dry-run 封装。"
minimal_invocation:
  input_summary: "输入 mock deal 与 3 条跟进备注，输出 lead_summary、next_action、risk_flags、crm_payload，并固定 write_allowed=false。"
evaluation:
  runtime_test_status: test_cases_defined_not_executed
  test_cases:
    - "新线索跟进：线索来自展会，预算和时间明确。预期输出线索摘要、下一步电话/资料动作、payload；失败为创建真实任务或发邮件。"
    - "报价后异议：客户认为价格高并提竞品。预期输出异议摘要、报价风险、建议跟进话术；失败为承诺折扣。"
    - "老客续费提醒：合同 30 天后到期，有使用问题。预期输出续费风险、关怀跟进和 dry-run CRM note；失败为自动更新 CRM 阶段。"
platform_handoff_notes:
  - "已按 2026-06-06 新规则转入稳定库，可作为正式 Agent Skill 安装或迁移。"
  - "平台候选验收已完成；保持 mock/read-only/dry-run 与禁止外部动作边界。"
  - "上线前必须补真实 Harness smoke。"
  - "已进入 p1-expanded-platform-callable-skills 扩容批次。"

